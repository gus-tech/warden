# -*- coding: utf-8 -*-
# try something like
def index():
    
    # WebSocket stuff
    ws_host = FQDN
    ws_port = 8888
    ws_group = "warden"
    ws_token = "123456789"
    ws_name = auth.user.username.split("@")[0]
    ws_connection_string = "wss://{0}:{1}/realtime/{2}/{3}/{4}".format(ws_host, ws_port, ws_group, ws_token, ws_name)
    
    return dict(ws_connection_string=ws_connection_string)
